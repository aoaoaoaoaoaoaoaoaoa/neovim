"git clone https://github.com/aoaoaoaoaoaoaoaoaoa/neovim ~/.config/neovim"

to start:
1) nvim
2) "Lazy sync", shift+i
3) after installation — shift+s
4) :Mason — install what you need
5) install fd or something if needed, to check — checkhealth

done

files:
~/.config/nvim/
~/.local/share/nvim
~/.local/state/nvim
